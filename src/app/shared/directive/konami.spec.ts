import { Konami } from '@webows/shared/directive/konami';

describe('Konami', () => {
  it('should create an instance', () => {
    const directive = new Konami();
    expect(directive).toBeTruthy();
  });
});
